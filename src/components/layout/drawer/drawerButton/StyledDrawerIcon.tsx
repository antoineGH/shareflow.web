import { styled } from '@mui/material/styles'
import ListItemIcon from '@mui/material/ListItemIcon'

const StyledDrawerIcon = styled(ListItemIcon)({
  minWidth: '2rem',
})

export default StyledDrawerIcon
